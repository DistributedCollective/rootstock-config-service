apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: '1'
  labels:
    app: rootstock-config-service
  name: rootstock-config-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rootstock-config-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: rootstock-config-service
    spec:
      containers:
        - image: sovryn/safe-config
          imagePullPolicy: Always
          envFrom:
          - secretRef:
              name: rootstock-config-service
          - secretRef:
              name: rootstock-config-service-secret
          name: web
          resources: {}
          tty: true
          volumeMounts:
            - mountPath: /nginx
              name: nginx-shared
        - image: nginx:1.20-alpine
          name: nginx
          envFrom:
          - secretRef:
              name: rootstock-config-service
          - secretRef:
              name: rootstock-config-service-secret
          ports:
            - containerPort: 80
          resources: {}
          volumeMounts:
            - name: nginxconf
              mountPath: /etc/nginx/templates
            - mountPath: /nginx
              name: nginx-shared
      restartPolicy: Always
      volumes:
        - name: nginx-shared
          emptyDir: {}
        - name: nginxconf
          secret:
            secretName: rootstock-config-service-nginx
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: rootstock-config-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: http
    service.beta.kubernetes.io/aws-load-balancer-ssl-cert: arn:aws:acm:us-east-2:768438872063:certificate/9fa02b26-0ee5-4301-bdd6-8474344885ff
spec:
  selector:
    app: rootstock-config-service
  ports:
    - port: 443
      targetPort: 80
  type: LoadBalancer
---
